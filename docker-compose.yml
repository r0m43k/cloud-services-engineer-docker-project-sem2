version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8081:8081"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:8080"
    restart: unless-stopped